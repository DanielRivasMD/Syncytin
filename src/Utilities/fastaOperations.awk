#! /bin/awk -f

################################################################################

BEGIN {
}

################################################################################

{
  # identify record id
  if ( /^>/ ) {

    # count records
    N++;

    # select record
    if ( N == line ) {

      # print id
      printf("%s%s\n", ( N > 0 ? "\n" : "" ), $0);
      next;
    }

  # identify no id - sequence
  } else {

    # select record
    if ( N == line ) {

      # print sequence
      printf("%s", $0);
    }
  }
}

################################################################################

# add line break
END {
  print;
}

################################################################################
