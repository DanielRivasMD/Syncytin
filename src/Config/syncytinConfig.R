################################################################################

# project
projDir <- '/Users/drivas/Factorem/Syncytin'

################################################################################

# source
utilDir <- paste0( projDir, '/src/Utilities' )

################################################################################

# data
dataDir <- paste0( projDir, '/data' )

alignmentDir <- paste0( dataDir, '/alignmentMultipleSequence' )
annotationDir <- paste0( dataDir, '/annotationSource' )
assemblyReadmeDir <- paste0( dataDir, '/assemblyREADME' )
candidateDir <- paste0( dataDir, '/candidateSyncytinExtraction' )
diamondDir <- paste0( dataDir, '/diamondOutput' )
DNAzooDir <- paste0( dataDir, '/DNAzooSource' )
insertionDir <- paste0( dataDir, '/insertionSequenceExtraction' )
ncbiDir <- paste0( dataDir, '/ncbiAssemblyAnnotationSource' )
phylogenyDir <- paste0( dataDir, '/phylogenyTimeTree' )
predictionDir <- paste0( dataDir, '/predictionAlphaFold' )
profileDir <- paste0( dataDir, '/profileProtein' )
statsDir <- paste0( dataDir, '/statsMiscellaneous' )
syntenyDir <- paste0( dataDir, '/syntenyAnnotationExtraction' )
taxonomistDir <- paste0( dataDir, '/taxonomistIDExtraction' )
wasabiDir <- paste0( dataDir, '/wasabiScrappedSource' )

databaseDir <- paste0( dataDir, '/syncytinDataBase' )
accNDir <- paste0( databaseDir, '/accessionN' )
accPDir <- paste0( databaseDir, '/accessionP' )
genBankDir <- paste0( databaseDir, '/genBank' )

################################################################################

# list
listDir <- paste0( projDir, '/list')

################################################################################

# assembly
DNAzooList <- paste0( listDir, '/DNAzooList.csv')
ncbiList <- paste0( listDir, '/ncbiList.csv')

################################################################################
